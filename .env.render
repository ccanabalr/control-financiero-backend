# Variables de Entorno para Render
# ================================
# INSTRUCCIONES:
# 1. NO subir este archivo a Git (es solo referencia)
# 2. En Render Dashboard → Environment, copiar cada valor
# 3. Valores deben venir de:
#    - MongoDB Atlas (URL de conexión)
#    - Firebase serviceAccountKey.json (credenciales)

# MongoDB Connection String
# Obtén de: MongoDB Atlas → Connect → Connection String
# Formato: mongodb+srv://usuario:password@cluster.mongodb.net/control-financiero?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://usuario:password@cluster0.mongodb.net/control-financiero?retryWrites=true&w=majority

# Firebase Project ID
# Obtén de: Firebase Console → Project Settings → General
# Formato: nombre-del-proyecto-id
FIREBASE_PROJECT_ID=control-financiero-xxxxx

# Firebase Private Key ID
# Obtén de: serviceAccountKey.json → private_key_id
FIREBASE_PRIVATE_KEY_ID=abc123def456abc123def456abc123def456abc

# Firebase Private Key (IMPORTANTE: incluir saltos de línea \n)
# Obtén de: serviceAccountKey.json → private_key
# Formato multi-línea con \n entre líneas
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEF...\n-----END PRIVATE KEY-----\n

# Firebase Client Email
# Obtén de: serviceAccountKey.json → client_email
# Formato: firebase-adminsdk-xxxxx@project-id.iam.gserviceaccount.com
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-abc123@control-financiero-xxxxx.iam.gserviceaccount.com

# Firebase Client ID
# Obtén de: serviceAccountKey.json → client_id
# Formato: número de 21 dígitos
FIREBASE_CLIENT_ID=123456789012345678901

# Spring Boot Profiles
# Valor: production
SPRING_PROFILES_ACTIVE=production

# Puerto del servidor
# Valor: 8080 (Render lo asignará automáticamente)
SERVER_PORT=8080
